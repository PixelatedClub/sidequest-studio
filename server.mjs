import http from 'node:http';import {readFile} from 'node:fs/promises';
const server=http.createServer(async(req,res)=>{if(req.url!=='/'&&req.url!=='/index.html'){res.writeHead(404);return res.end('Not found');}try{const html=await readFile(new URL('Sidequest Studio.html',import.meta.url));res.writeHead(200,{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-store','X-Content-Type-Options':'nosniff'});res.end(html);}catch{res.writeHead(500);res.end('Run node build.mjs first.');}});server.listen(Number(process.env.PORT||4320),'127.0.0.1',()=>console.log('Sidequest Studio: http://localhost:'+server.address().port));
